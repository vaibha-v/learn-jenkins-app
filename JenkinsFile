pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'echo "Hello from Pipeline Proj"'
                sh 'pwd'
                sh 'whoami'
            }
        }
    }
}
